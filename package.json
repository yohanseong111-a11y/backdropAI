{
  "name": "backshotai-web",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-models.mjs && vite build",
    "preview": "vite preview",
    "lint": "node --check src/main.js && node --check src/removal-worker.js && node --check src/mask-safety.js && node --check src/mask-refine.js && node --check src/assist.js",
    "test": "node --test"
  },
  "dependencies": {
    "jszip": "3.10.1",
    "onnxruntime-web": "1.21.0",
    "@huggingface/transformers": "3.7.1"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
