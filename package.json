{
  "name": "backshotai-web",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-models.mjs && vite build",
    "preview": "vite preview",
    "lint": "node --check src/main.js && node --check src/removal-worker.js && node --check src/refine-worker.js && node --check src/mask-safety.js && node --check src/mask-refine.js && node --check src/mask-recover.js && node --check src/assist.js",
    "test": "node --test",
    "verify:cutout": "node scripts/verify-cutout.mjs",
    "verify:real-jacket": "node scripts/verify-real-jacket.mjs",
    "verify:app": "node scripts/verify-app.mjs"
  },
  "dependencies": {
    "@huggingface/transformers": "3.7.1",
    "jszip": "3.10.1",
    "onnxruntime-web": "1.21.0"
  },
  "devDependencies": {
    "puppeteer-core": "23.11.1",
    "vite": "^7.1.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
